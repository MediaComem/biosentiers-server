.modal-header
  h3.modal-title Connexion

// Login form
form(name='loginModalCtrl.form', ng-submit='loginModalCtrl.form.$valid && loginModalCtrl.login()', novalidate)
  .modal-body

    // User e-mail
    .form-group(validates='loginModalCtrl.form.email')
      label.control-label(for='user-email') Adresse e-mail
      input#user-email.form-control(type='text', ng-model='loginModalCtrl.user.email', name='email', required, autofocus)

      // Error: the e-mail is missing
      validation-error(for='loginModalCtrl.form.email', validation='required')
        | Merci d'entrer votre adresse e-mail.

    // User password
    .form-group(validates='loginModalCtrl.form.password')
      label.control-label(for='user-password') Mot de passe
      input#user-password.form-control(type='password', ng-model='loginModalCtrl.user.password', name='password', required)

      // Error: the password is missing
      validation-error(for='loginModalCtrl.form.password', validation='required')
        | Merci d'entrer votre mot de passe.

    // Error: the e-mail or password is incorrect
    p.alert.alert-warning(ng-if='loginModalCtrl.apiError && loginModalCtrl.apiError.status == 401')
      | Votre adresse e-mail ou votre mot de passe est incorrect.

  // Form controls
  .modal-footer
    .btn-group
      button.btn.btn-default(type='button', ng-click='loginModalCtrl.dismiss()') Annuler
      button.btn.btn-primary(type='submit', ng-disabled='loginModalCtrl.form.$invalid') Connecter
